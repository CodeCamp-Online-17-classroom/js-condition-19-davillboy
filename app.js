// write code here
 <script>
        let score = prompt('ใส่คะแนนของคุณ');
        let grade =
        (score >= 80) ? 'A' :
        (score >= 70) ? 'B' :
        (score >= 60) ? 'C' :
        (score >= 50) ? 'D' :
        'F';

        alert(`เกรดที่ได้: ${grade}`);

  </script>
